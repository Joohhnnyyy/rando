# Python Version: 3.11.13 (ensure it's set in runtime.txt if using Render)

# Core FastAPI and Server
fastapi==0.109.2
uvicorn==0.27.1
python-multipart==0.0.6

# Data Validation and Environment
pydantic[email]==2.6.1
python-dotenv==1.0.0

# Data Processing and Analysis
numpy==1.24.3         # ✅ Downgrade from 2.x to avoid TensorFlow issues
pandas>=1.3.0
scikit-learn==1.3.2    # ✅ Stick to 1.x as 1.6.1 is not available yet in PyPI
joblib==1.3.2

# Machine Learning and Gemini
tensorflow==2.15.0     # ✅ Use 2.15.0 for best compatibility (avoid 2.18.0 unless sure)
google-generativeai==0.3.2  # ✅ Pin to a known version

# Image Processing
Pillow==10.3.0         # ✅ Always pin version

# Build Tools
setuptools>=65.5.0
wheel 